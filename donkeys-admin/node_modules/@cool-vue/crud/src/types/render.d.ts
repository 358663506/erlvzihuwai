import { ComponentOptions } from "vue";

declare interface Options extends ComponentOptions {
	name: string;

	options?:
		| Array<{
				label: string;
				value?: any;
				[key: string]: any;
		  }>
		| Function;

	props?: any;
}

declare interface CustomButton {
	label: string;
	onClick({ scope }): void;
}

export declare type RenderOptions = Options | Function;

export declare type RenderOpButton = `slot-${string}` | CustomButton;
