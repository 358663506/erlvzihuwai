import { AccessToken } from '@tnwx/accesstoken';
/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 企业微信 API 接口
 */
export declare class QyWxApi {
    private static updateTaskCardUrl;
    /**
     * 更新任务卡片消息状态
     * @param userIds 企业的成员ID列表（消息接收者，最多支持1000个）
     * @param agentId 应用的 agentId
     * @param taskId 发送任务卡片消息时指定的 taskId
     * @param clickedKey 设置指定的按钮为选择状态，需要与发送消息时指定的btn:key一致
     * @param accessToken AccessToken
     */
    static updateTaskCard(userIds: string, agentId: string, taskId: string, clickedKey: string, accessToken?: AccessToken): Promise<any>;
    private static getApiDomainIpUrl;
    /**
     * 获取企业微信API域名IP段
     */
    static getApiDomainIp(accessToken?: AccessToken): Promise<any>;
}
