import { BaseMsg } from '../BaseMsg';
/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 注册完成回调事件
 */
export declare class InRegisterCorp extends BaseMsg {
    static INFO_TYPE: string;
    private servicecorpid;
    private infotype;
    private timestamp;
    private registercode;
    private authcorpid;
    private accesstoken;
    private expiresin;
    private userid;
    private externaluserid;
    private state;
    private templateid;
    constructor(serviceCorpId: string, infoType: string, timeStamp: number, registerCode: string, authCorpId: string, accessToken: string, expiresIn: number, userId: string, state: string, templateId: string);
    get serviceCorpId(): string;
    set serviceCorpId(serviceCorpId: string);
    get infoType(): string;
    set infoType(infoType: string);
    get timeStamp(): number;
    set timeStamp(timeStamp: number);
    get registerCode(): string;
    set registerCode(registerCode: string);
    get authCorpId(): string;
    set authCorpId(authCorpId: string);
    get accessToken(): string;
    set accessToken(accessToken: string);
    get expiresIn(): number;
    set expiresIn(expiresIn: number);
    get userId(): string;
    set userId(userId: string);
    get getState(): string;
    set setState(state: string);
    get templateId(): string;
    set templateId(templateId: string);
}
