import { BaseMsg } from '../BaseMsg';
/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 推送suite_ticket
 */
export declare class InSuiteTicket extends BaseMsg {
    static INFO_TYPE: string;
    private suiteid;
    private infotype;
    private timestamp;
    private suiteticket;
    constructor(suiteId: string, infoType: string, timeStamp: number, suiteTicket: string);
    get suiteId(): string;
    set suiteId(suiteId: string);
    get infoType(): string;
    set infoType(infoType: string);
    get timeStamp(): number;
    set timeStamp(timeStamp: number);
    get suiteTicket(): string;
    set suiteTicket(suiteTicket: string);
}
