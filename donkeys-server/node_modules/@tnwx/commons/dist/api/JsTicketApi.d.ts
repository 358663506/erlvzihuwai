/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 获取api_ticket
 */
import { AccessToken } from '@tnwx/accesstoken';
import { JsTicket } from '../entity/JsTicket';
import { JsApiType } from '../Enums';
export declare class JsTicketApi {
    private static getTicketUrl;
    /**
     * 获取api_ticket
     * @param type
     * @param accessToken api_authorizer_token
     */
    static getTicket(type: JsApiType, accessToken?: AccessToken): Promise<JsTicket>;
    /**
     * 检测jsTicket是否有效
     * @param jsTicketJson
     */
    static isAvailable(jsTicketJson: string): boolean;
}
