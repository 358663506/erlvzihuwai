import { AccessToken } from '@tnwx/accesstoken';
import { SubscribeMsg } from '@tnwx/commons';
/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 一次性订阅消息
 */
export declare class SubscribeMsgApi {
    private static authorizeUrl;
    private static subscribeUrl;
    /**
     * 获取授权URL
     * @param scene  0-10000 场景值
     * @param templateId 订阅消息模板ID
     * @param redirectUrl
     * @param reserved 可以填写a-zA-Z0-9的参数值
     */
    static getAuthorizeURL(scene: number, templateId: string, redirectUrl: string, reserved?: string): string;
    /**
     * 推送订阅模板消息给到授权微信用户
     * @param subscribeMsg
     * @param accessToken
     */
    static send(subscribeMsg: SubscribeMsg, accessToken?: AccessToken): Promise<any>;
    /**
     * 推送订阅模板消息给到授权微信用户
     * @param json
     * @param accessToken
     */
    static sendMsg(json: string, accessToken?: AccessToken): Promise<any>;
}
