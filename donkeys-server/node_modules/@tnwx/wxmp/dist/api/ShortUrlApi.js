"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const util = require("util");
const kits_1 = require("@tnwx/kits");
const accesstoken_1 = require("@tnwx/accesstoken");
/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 长链接转成短链接
 */
class ShortUrlApi {
    /**
     * 长链接转成短链接
     * @param json
     * @param accessToken
     */
    static getShorturl(json, accessToken) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!accessToken) {
                accessToken = yield accesstoken_1.AccessTokenApi.getAccessToken();
            }
            let url = util.format(this.apiUrl, accessToken.getAccessToken);
            return kits_1.HttpKit.getHttpDelegate.httpPost(url, json);
        });
    }
    /**
     * 长链接转成短链接
     * @param longUrl
     * @param accessToken
     */
    static longToShort(longUrl, accessToken) {
        return __awaiter(this, void 0, void 0, function* () {
            return this.getShorturl(JSON.stringify({
                action: 'long2short',
                long_url: longUrl
            }), accessToken);
        });
    }
}
exports.ShortUrlApi = ShortUrlApi;
ShortUrlApi.apiUrl = 'https://api.weixin.qq.com/cgi-bin/shorturl?access_token=%s';
//# sourceMappingURL=ShortUrlApi.js.map