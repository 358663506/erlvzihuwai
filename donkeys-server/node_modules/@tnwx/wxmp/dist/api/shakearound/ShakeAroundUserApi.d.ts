import { AccessToken } from '@tnwx/accesstoken';
/**
 * @author Javen
 * @copyright javendev@126.com
 * @description 获取设备及用户信息
 */
export declare class ShakeAroundUserApi {
    private static getShakeInfoUrl;
    /**
     * 获取设备及用户信息
     * @param ticket 摇周边业务的ticket，可在摇到的URL中得到，ticket生效时间为30分钟，每一次摇都会重新生成新的ticket
     * @param needPoi 是否需要返回门店poi_id
     * @param accessToken
     */
    static getShakeInfo(ticket: string, needPoi?: boolean, accessToken?: AccessToken): Promise<any>;
}
