import { IMidwayApplication } from "@midwayjs/core";
import { CoolPlugin, PLUGINSTATUS } from "@cool-midway/core";
import { ICoolAliPay } from ".";
/**
 * 支付
 */
export declare class Sdk implements ICoolAliPay {
    coolPlugin: CoolPlugin;
    app: IMidwayApplication;
    pay: any;
    aliConfig: any;
    /**
     * 初始化
     */
    init(): Promise<PLUGINSTATUS.NOCONF | PLUGINSTATUS.USABLE>;
    /**
     * 下载文件
     * @param path
     * @param url
     */
    download(path: string, url: string): Promise<unknown>;
    /**
     * 检查状态
     * @returns
     */
    checkStatus(): Promise<PLUGINSTATUS.NOCONF | PLUGINSTATUS.USABLE>;
    /**
     * 微信支付
     * @returns
     */
    getInstance(): any;
    /**
     * 签名
     * @param req
     * @returns
     */
    signVerify(req: any): any;
    /**
     * 创建订单
     */
    createOrderNum(length?: number): any;
    /**
     * 动态配置支付参数
     * @param config
     * @returns
     */
    initPay(config: any): any;
}
