import { IMidwayApplication } from "@midwayjs/core";
import { CoolPlugin, ICoolPlugin, PLUGINSTATUS } from "@cool-midway/core";
import { RedisCacheHandler } from "@cool-midway/redis";
import { ILogger } from "@midwayjs/logger";
import { BaseCoolQueue } from "./base";
/**
 * 任务调度
 */
export declare class CoolQueue implements ICoolPlugin {
    app: IMidwayApplication;
    coreLogger: ILogger;
    coolPlugin: CoolPlugin;
    redisCacheHandler: RedisCacheHandler;
    private redisConfig;
    init(): Promise<PLUGINSTATUS.NOCONF | PLUGINSTATUS.USABLE | PLUGINSTATUS.UNKNOWNERR>;
    scan(): Promise<void>;
    /**
     * 检查插件状态
     */
    checkStatus(): Promise<PLUGINSTATUS.NOCONF | PLUGINSTATUS.USABLE | PLUGINSTATUS.UNKNOWNERR>;
    /**
     * 队列名称
     * @param name
     */
    createQueue(cls: BaseCoolQueue, name: string, mod: any): Promise<void>;
}
