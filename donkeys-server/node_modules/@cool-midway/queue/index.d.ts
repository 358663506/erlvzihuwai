import { Job, DoneCallback, Queue } from 'bull';
export { AutoConfiguration as Configuration } from './configuration';
export * from './base';
export * from './queue';
export * from './decorator/queue';
export interface ICoolQueue {
    data(job: Job, done: DoneCallback): void;
}
export interface IQueue {
    queue: Queue;
    data(job: Job, done: DoneCallback): void;
}
