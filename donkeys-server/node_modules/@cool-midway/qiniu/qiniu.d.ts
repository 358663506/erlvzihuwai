import { IMidwayApplication } from '@midwayjs/core';
import { ILogger } from '@midwayjs/logger';
import { CoolPlugin, ICoolFile, MODETYPE, PLUGINSTATUS } from '@cool-midway/core';
export declare class QiniuHandler implements ICoolFile {
    coolPlugin: CoolPlugin;
    app: IMidwayApplication;
    coreLogger: ILogger;
    namespace: string;
    init(): Promise<PLUGINSTATUS.NOCONF | PLUGINSTATUS.USABLE>;
    private createKey;
    upload(ctx: any): Promise<unknown>;
    checkStatus(): Promise<PLUGINSTATUS.NOCONF | PLUGINSTATUS.USABLE>;
    getMode(): {
        mode: MODETYPE;
        type: string;
    };
    getMetaFileObj(): {
        mode: MODETYPE;
        type: string;
    };
}
