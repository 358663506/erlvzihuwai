import { EsConfig } from "./decorator/elasticsearch";
import { IMidwayApplication } from "@midwayjs/core";
import { CoolEventManager, CoolPlugin, ICoolPlugin, PLUGINSTATUS } from "@cool-midway/core";
import { ILogger } from "@midwayjs/logger";
/**
 * 搜索引擎
 */
export declare class CoolElasticSearch implements ICoolPlugin {
    app: IMidwayApplication;
    coreLogger: ILogger;
    coolPlugin: CoolPlugin;
    private esConfig;
    private client;
    coolEventManager: CoolEventManager;
    init(): Promise<PLUGINSTATUS>;
    scan(): Promise<void>;
    /**
     * 检查插件状态
     */
    checkStatus(): Promise<PLUGINSTATUS>;
    /**
     * 数据更新事件
     * @param method
     * @param data
     */
    esDataChange(method: any, data: any): Promise<void>;
    /**
     * 创建索引
     * @param cls
     * @param config
     */
    createIndex(cls: any, config: EsConfig, providerId: any, module: any): Promise<void>;
}
